spring:
  application:
    name: aspect-example
  cloud:
    config:
#      uri: ${vcap.services.${PREFIX:}configserver.credentials.uri:http://user:password@localhost:8888}
      enabled: false
    discovery:
      enabled: true

eureka:
  client:
    enabled: true
    healthcheck:
      enabled: true